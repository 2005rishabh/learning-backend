<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Edit User</title>
</head>
<body class="bg-zinc-900 text-white flex items-center justify-center min-h-screen">
  <form action="/edit/<%= user._id %>" method="post" class="p-6 bg-zinc-800 rounded-lg w-full max-w-md">
    <h1 class="text-2xl mb-4">Edit User</h1>
    <input type="text" name="name" value="<%= user.name %>"
      class="w-full mb-3 p-2 rounded bg-zinc-700" />
    <input type="email" name="email" value="<%= user.email %>"
      class="w-full mb-3 p-2 rounded bg-zinc-700" />
    <input type="text" name="image" value="<%= user.image %>"
      class="w-full mb-3 p-2 rounded bg-zinc-700" />
    <button type="submit" class="px-4 py-2 bg-blue-500 rounded">Update</button>
  </form>
</body>
</html>
