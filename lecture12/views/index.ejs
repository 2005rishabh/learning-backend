<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
  </head>
  <body class="bg-zinc-900 text-white">
    <div class="w-full min-h-screen flex items-center justify-center">
      <div class="nav mb-10">
        <a href="/read">Read Users</a>
      </div>
      <div class="w-full max-w-4xl p-6">
        <h1 class="text-2xl font-semibold mb-4">Create User</h1>

        <!-- Inline form -->
        <form action="/create" method="post" class="flex gap-3">
          <input
            type="text"
            class="flex-1 px-4 py-2 border border-zinc-700 rounded-lg bg-zinc-800 focus:border-blue-400 outline-none"
            name="name"
            placeholder="Name"
          />
          <input
            type="email"
            class="flex-1 px-4 py-2 border border-zinc-700 rounded-lg bg-zinc-800 focus:border-blue-400 outline-none"
            name="email"
            placeholder="Email"
          />
          <input
            type="text"
            class="flex-1 px-4 py-2 border border-zinc-700 rounded-lg bg-zinc-800 focus:border-blue-400 outline-none"
            name="image"
            placeholder="Image URL"
          />
          <button
            type="submit"
            class="px-6 py-2 bg-blue-500 hover:bg-blue-600 text-white font-medium rounded-lg transition"
          >
            Create User
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
