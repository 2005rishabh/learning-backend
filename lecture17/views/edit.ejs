<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Edit Post</title>
  </head>
  <body class="bg-zinc-900 text-white p-10">
    <h2 class="text-2xl mb-5">Edit Post</h2>
    <form action="/edit/<%= post._id %>" method="post">
      <textarea
        name="content"
        class="p-2 block mb-4 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
      >
<%= post.content %></textarea
      >
      <input
        type="submit"
        value="Update"
        class="px-3 py-2 bg-blue-600 rounded-md cursor-pointer"
      />
    </form>
  </body>
</html>
